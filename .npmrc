save-exact=true
ignore-scripts=true
min-release-age=7
# npm 12 blocks git dependencies by default (EALLOWGIT). "root" allows git
# deps declared in this project's package.json (waste-movement-utils) while
# still blocking transitive git deps.
allow-git=root
